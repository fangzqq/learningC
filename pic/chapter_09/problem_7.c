#include <stdio.h>

void insertString(char source[], char text[], int index)
{
    int i, lens = 0, lent = 0;

    for (i = 0; source[index + i] != '\0'; ++i);
    lens = i - 1;

    for (i = 0; text[i] != '\0'; ++i);
    lent = i - 1;


        
}